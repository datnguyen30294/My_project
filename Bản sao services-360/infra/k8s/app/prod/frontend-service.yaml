apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: residential-management-prod
  labels:
    app.kubernetes.io/name: residential-management
    app.kubernetes.io/component: frontend
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30082
      name: http
  selector:
    app.kubernetes.io/name: residential-management
    app.kubernetes.io/component: frontend
